-- Passwordless sudo for a length of time you pick (~/.local/bin/sudo-timer).
-- Opens the Omarchy menu straight at the submenu with the durations.
o.bind("SUPER + SHIFT + U", "Passwordless sudo", "omarchy-menu toggle setup.security.passwordless-sudo")

-- Lösenordsfri sudo med valbar tid (~/.local/bin/sudo-timer).
-- Öppnar Omarchy-menyn direkt på undermenyn med tiderna.
o.bind("SUPER + SHIFT + U", "Lösenordsfri sudo", "omarchy-menu toggle setup.security.passwordless-sudo")
